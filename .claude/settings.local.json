{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(tsx|ts|css|json)$' && cd C:/Users/minse/vazant-dashboard-v2 && npx vercel --yes --prod > /dev/null 2>&1; } || true",
            "timeout": 120,
            "async": true,
            "statusMessage": "Deploying to Vercel..."
          }
        ]
      }
    ]
  }
}
