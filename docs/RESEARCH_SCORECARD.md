# Research-AI Scorecard — re-grade after case-law + CI-gate + over-abstention work (2026-06-25 PM)

> 7 independent read-only auditors graded the LIVE code; a skeptical EA synthesizer verified load-bearing
> claims against the code (not the markdown) and downgraded generous self-assessments.

| Dimension | Prior | **Now** | Target | Hit |
|---|---|---|---|---|
| retrieval | A- | **A-** | A | no |
| grounding | A- | **A** | A | yes |
| deterministic-numbers-eval | B+ | **A-** | A | no |
| calibration | A- | **A-** | A | no |
| research-to-execution-trust | B | **A-** | A | no |
| authority-weighting | A- | **B+** | A- | no |
| primary-authority-coverage | B+ | **A-** | A- | yes |

**At target: 3/7 (was 1). Clears the 5-of-6 bar: NO.**

## Verdict

Not an A engine yet — it is a strong B+ prototype with a now-genuinely-enforced safety moat, and that is real progress, not a regrade illusion. I verified the load-bearing claims against the code rather than the markdown: the deterministic recall gate passes live (32 tests green), the research-confidence-to-risk wiring is real (runtime.ts:272 -> risk.ts:49-56) and tested, the Opus freshness judge is actually instantiated in the production API route, the figure-gate regex and fabrication guard are in place, the authority honesty-cap is enforced, the 12 case-law chunks are web-verified and live, and the engine code was NOT edited after the 97.4% measurement (the 49/50 number is honestly attached to shipped code). So the prior 'strong-B, 1 of 7 at target' has legitimately become 3 of 7 at target. But it does NOT clear the user's '5 of 6' bar, and a skeptic must down-grade three generous self-assessments: (1) authority-weighting is B+, not the A- the input narrative credited as 'the one at target' — contraSearched is never set true in live code, so the MLTN cap is permanent and the in-circuit invariant is tested only on synthetic inputs; (2) the eval dimension's live-model gate is wired but DORMANT (skips until an ANTHROPIC_API_KEY secret with quota headroom is added) so today only the model-free recall gate actually blocks; (3) research-to-execution is wired and unit-tested but has NEVER run on a real agent loop with real Claude + a real return (the test uses synthetic scope and a mock tool). The three still-aspirational pieces are exactly the ones the user flagged: corpus BREADTH (federal-only, 72 chunks, zero entity/multistate/circuit-split), the contra-authority finder, and the production agent-OS exercise. The accuracy number is real but narrow: 97.4% on a thin 51-case OBBBA+doctrine set measured on production Claude (Claude-specific, not model-agnostic), not a general tax-research claim. Verdict: a safety architecture that earns trust (it fails SAFE — abstains, does not hallucinate) sitting on a corpus too thin to claim domain mastery. Strong B+, one secret-activation and one real agent run away from a defensible 'A on the controllables.'

## Per-dimension

- **retrieval: A- → A-** (target A) — Verified real: hybrid filter-then-rank (store.ts:148-153), fabrication guard wired (engine.ts:586), deterministic recall gate passes live (ran it: 32 tests green, >=30 answer-cases guarded). But retrieval MECHANICS did not improve to A — the 86.8%->97.4% climb came from CORPUS work + the figure-gate fix, not retrieval. Keyword match is still brittle substring (store.ts:131, q.includes(kw)); graph RRF built but default-off (engine.ts:567 gates on PETAL_GRAPH_RETRIEVAL=1) and measured STALE at 86.8% because corpus edits were never backfilled to the graph DB. The retrieval layer itself is A-, not A.
- **grounding: A- → A** (target A, HIT) — The strongest dimension and genuinely at target. All five gates verified in code: no-citation-no-claim, citation re-resolution, value-level figure gate (MONEY_RE at engine.ts:304, regex fix confirmed), faithfulness pre-filter, and a SEPARATE Opus judge that is actually wired into the production API route (route.ts: judge=getProvider('claude-opus-4-8'), distinct from the Sonnet proposer). Honest service-error degradation is real (never silent). 2% measured error, zero fabrications in live runs. Residual gaps (judge only on answer bucket, federal-only set) are real but do not block an A.
- **deterministic-numbers-eval: B+ → A-** (target A) — Big real move: the moat went from a markdown number to an ENFORCED per-PR deterministic recall gate (retrieval-gate.test.ts, ran it, passes; commit 3010efa). Engine code was NOT touched after the 97.4% confirm commit (verified: git diff dd065f2..HEAD over engine.ts + corpus-obbba.ts is empty), so the shipped code matches the measured number. NOT A: the LIVE-model gate is wired but DORMANT — research-eval.yml line 26 skips unless ANTHROPIC_API_KEY secret exists; the 44/50 floor blocks nothing today. Only the model-free recall gate actually gates. Quota was exhausted mid-measurement; no headroom confirmed. 50-case set is thin vs Blue J 350+.
- **calibration: A- → A-** (target A) — Real fix: the over-abstention cluster on settled OBBBA law was root-caused (chunks carried rules but not pre-computed figures) and corrected with explicit figures (commits 2c7a3b8, 68834b1), validated by the 86.8%->97.4% climb. Four-bucket + six-reason contract is real. NOT A: one case (tips-deduction-exists-2025) still sits flaky at the answer/abstain margin by the engine's own account; post-expiration lifecycle reasoning (tips-sunset-2029) is a genuine capability gap, not gameable; unsettled/circuit-split reason is dormant (no non-final authority tier in corpus). These are honest reasoning-layer gaps, not missing gates.
- **research-to-execution-trust: B → A-** (target A) — Biggest honest jump and the wiring is REAL, not aspirational: runtime.ts:272 calls classifyRisk(tool, args, lastResearchSignals) on tier-3 staging; risk.ts:49-56 lowConfidence demotes confirm->review on abstain/coverage_gap/recon-mismatch/conf<0.6; the integration test passes (ran it). NOT A purely on scope-of-evidence: the demote-on-hedge path has NEVER been exercised on a real agent loop with real Claude + a real return — research-confidence-gate.test.ts uses taxScope:'synthetic' and a mock tool. The gate is production-ready but unmeasured end-to-end. One real run closes it.
- **authority-weighting: A- → B+** (target A-) — DOWNGRADED from the prior self-claimed A-. The dimension's own currentGrade is B+ against an A- target — it does NOT hit target, correcting the input narrative that listed it as the one dimension at target. What is real: assessAuthorityWeight is wired (engine.ts:492,823), the honesty cap is enforced and load-bearing (authority-assess.ts:52 caps MLTN->substantial-authority when !contraSearched), 12 real web-verified cases, weighting unit tests pass (ran them). Why B+: contraSearched is NEVER set true in any live call site (grep confirms only the signature + test), so the MLTN cap is permanent; the in-circuit controlling-holding invariant is proven only on synthetic inputs — no real circuit split in the corpus. The honesty is admirable but it caps the grade.
- **primary-authority-coverage: B+ → A-** (target A-, HIT) — At its (relaxed) target. Real delivery: 12 web-verified case-law doctrines (corpus-caselaw.ts, all precedential, verified vs Cornell LII/Justia), now live in REGISTERED_CORPUS and exercised by golden cases that pass 12/12 on production Claude. Corpus is 72 chunks; golden set is actually 51 cases (grep id: = 51, doc says 50). Honest A- not A: zero entity depth (no S-corp/partnership/C-corp), single non-federal jurisdiction (CA only), zero circuit splits, thin advanced-deduction coverage. Foundational doctrines, not nuanced holdings.

## Prioritized path to 5-of-6

1. Activate the live-model gate (highest leverage, lowest effort): add the ANTHROPIC_API_KEY repo secret with quota headroom so research-eval.yml's 44/50 floor actually blocks releases. This is the single change that flips eval from A- toward A — the code is already wired, it just skips today.

2. Exercise research-to-execution on ONE real loop: run a real agent task (synthetic firm/client is fine, but real Claude + a real staged tier-3 write) where a research sub-call returns hedge/abstain, and confirm the write demotes to the review lane. One run closes the only gap holding this dimension below A.

3. Wire the contra-authority finder honestly: set contraSearched=true only after a real contrary-authority retrieval runs, lifting the permanent MLTN cap and moving authority-weighting from B+ back to A-. Pair it with one REAL circuit-split scenario in the golden set so the in-circuit-controlling invariant is tested on a true conflict, not synthetic inputs.

4. Backfill the graph corpus and re-measure on production Claude: the graph is stale at 86.8% only because corpus edits were never propagated to authority_versions DB. Backfill, re-measure, and either flip it as default (gaining semantic density to fix lexical brittleness) or keep in-memory with evidence — this is what moves retrieval from A- to A.

5. Attack corpus BREADTH, the dimension the user named as still-aspirational: add 15-20 entity chunks (S-corp/partnership K-1, basis, passive loss) + 3-5 real multistate jurisdictions, with measured golden cases. This is the long pole — it is what separates 'strong moat on a thin set' from a genuine domain-A research engine, and no amount of gate-wiring substitutes for it.

6. Stabilize the tips-deduction-exists-2025 calibration flake and expand the golden set toward 100+ cases so the measured number stops being Claude-specific and thin (50 vs Blue J 350+).

