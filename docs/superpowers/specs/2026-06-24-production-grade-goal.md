Petal: enterprise-grade, SOC2-ready, multi-tenant AI-native tax SaaS for individual AND business/entity practice, and at core an AGENTIC OS the firm runs work on (skills, agents, recurring tasks, connected software) — not an assistant on a dashboard. Take it from demo to a product a real firm runs: every surface real, world-class tax research, agents that act with human-verified safety, SOC2-Type-II-survivable security. "Barely prod" is failure; bar is enterprise-grade.

RULE 1 NO MOCK SURFACES: every page/tab/card/button/channel wired to a real RLS-scoped query or real API. Zero fixtures-as-real, PRNG fake taxpayer data (Intake tab), localStorage/in-memory persistence (memory, assignment, recurring templates, notifications, queued skills), toast/setTimeout stubs (hand-to-Petal, portal/compose send, nudge), hardcoded "connected"/fake accounts, silent fixture fallbacks. Kept demo data is LABELED demo. Not done until data, writes, AND failure states are real.

RULE 2 VISUAL FROZEN: components render 1:1 (layout/classes/fonts/colors/spacing/motion/copy). Wiring = swap data source (fixture→real query, same shape) + add lib/supabase/actions/routes/tables + client-state→audited tables. NEVER restyle/rebuild a component; adapt shapes in the repo layer.

8 SLICES AT PROD BAR (end-to-end, not "built"): (1) identity+tenancy+intra-firm RBAC (2) core data, every surface DB-bound (3) documents+extraction wired to upload w/ typed-field validation (4) AI orchestration on real data behind the risk gate (5) connectors MCP-first, honest status, email receive/threading (6) OLT as DRAFT-ONLY agent w/ real invocation+promote-from-quarantine (7) payments + real firm invoicing/AR (8) portal flow on real rails + real portal messaging. Each passes an adversarial prod+security review.

SECURITY/SOC2 (every slice): RLS firm_id isolation + cross-tenant tests; intra-firm RBAC (preparer/reviewer/admin, row+action enforced, preparer-drafts/reviewer-approves, no self-approval); middleware auth (unauth /os/* redirects, never a fixture shell); ALL PII envelope-encrypted (SSN/bank AND memory/notes/task-text); §7216 PROVEN (counsel or documented accepted-risk + a no-leak test that real taxpayer data never hits a non-ZDR/uncleared path); ZDR-only + redacted prompts; append-only audit on every mutation; HONEST DEGRADATION (no silent fallbacks; surface real-vs-demo/AI-fallback/sync-failed/partial) + structured logging; SOC2+WISP docs mapped to real controls; upload hardening (server MIME allowlist + AV).

RESEARCH AI (defensibility, not just answers): individual+entity (1065 Sub-K, 1120/1120S Sub-C/S, basis, §1031) to reg/ruling/case DEPTH. Weight authority by the §6662 substantial-authority list (Treas Reg 1.6662-4(d)(3)(iii)), NOT a hand-rolled linear tier. Tag each authority: type, DELEGATION (post-Loper-Bright: express-delegation durable vs §7805 interpretive contestable, Skidmore not Chevron), jurisdiction+circuit, effective-date+supersession. Authority = 3 axes (statute; courts by level+circuit; agency) — NEVER a ruling over a contrary controlling court holding in-circuit; never cite PLR/TAM as precedent; flag low-weight-only positions. Corpus from IRC, eCFR, IRB, CourtListener via the grounded ingest pipeline (effective dates+supersession+grounding gate). Retrieval = metadata pre-filter → hybrid (semantic+keyword) → rerank + graph for multi-hop. lib/tax deterministic worksheets are SOURCE OF TRUTH for numbers (model never does arithmetic); OLT/Drake are cross-check+filing, never the engine. MOAT = a MEASURED error rate (LegalBench-RAG-anchored + EA-reviewed golden set) gating releases. Always cited, grounded, abstaining; never confidently wrong.

AGENTIC OS: one durable runtime (Postgres agent_tasks/runs), two entry points (Tasks hand-to-Petal + AI mode), versioned SKILLS, SCHEDULER (cron spawns recurring runs). Drivers MCP-FIRST: real MCP client (Xero/QBO plug in) > direct API > Stagehand/Playwright ONLY where no API/MCP (OLT); each connector declares modality+reliability; browser=higher-risk. RISK GATE (the §7216 determination in code): classify every action by reversibility × stakes(money/IRS/official-record) × connector-reliability × confidence(research tier+abstention, recon match) → AUTO (reads/research/draft/internal) or STAGED. DEFAULT = DRAFT-EVERYTHING, HUMAN COMMITS: no external side effect (send/create/request/post/file) without human approval via action_proposals (reviewer-approved). HIGH-STAKES EXTERNAL (file in OLT, post in Xero, money/IRS/official record) = DRAFT-ONLY: Petal prepares the draft + the HUMAN does the final irreversible submit — never auto-submitted even after approval. Low confidence/browser → mandatory line-by-line. Petal NEVER autonomously does an irreversible/high-liability step. VERIFICATION CHEAP: every staged/drafted action ships an EVIDENCED REVIEW ARTIFACT (each output field → its source: wage box→W-2 line 1, entry→bank txn) so review is a 30-sec check vs evidence, not a redo; ai_suggestions quarantine + recon evidence are the substrate.

PROCESS: committed specs/plans task-by-task, TDD+RLS-first, subagent-driven, in the backend worktree without breaking the live demo; E2E tests for seams (task lifecycle, upload→storage, extraction, agent SSE, risk-gate approval, portal, gcal→UI). Pause/ask ONLY for an external credential or legal decision (§7216 counsel, Stripe/GovInfo/MCP creds).

DONE WHEN: every surface real (zero mock as real; demo labeled); all 8 slices prod-grade; RBAC enforced+tested; all PII encrypted; §7216 proven w/ no-leak test; research AI at a stated MEASURED error rate w/ the §6662/Loper-Bright/jurisdiction authority model live (individual+entity); Petal runs as an agentic OS (recurring+handed tasks on the scheduler, MCP-first connectors, EVERY action through the risk gate — draft-everything/human-commits, high-stakes external draft-only w/ human submit — shipping evidenced artifacts); failures degrade honestly w/ observability; SOC2+WISP mapped; full isolation+security+E2E suite green; prod deploy renders 1:1 to the frozen design with real audited data behind every pixel.
